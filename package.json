{
  "name": "card-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:editor": "pnpm --filter @card-studio/editor dev",
    "dev:render": "pnpm --filter @card-studio/render dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "sync-frames": "node scripts/sync-frame-library.mjs",
    "sync-fonts": "node scripts/sync-font-library.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
